#include<iostream>
using namespace std;
int Fib1(int N) {
    if (N == 1||N==2) return 1;
    return Fib1(N - 1) + Fib1(N - 2);
}
int main()
{
    int N;
    cin  >> N;
    cout << Fib1(N);
    return 0;
}
