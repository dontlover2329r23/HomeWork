#include<iostream>
#include<string>
using namespace std;
unsigned long int Fact1(long long N) {
   if ((N == 1)||(N==0)) {
       return 1;
   }
   
   return N*Fact1(N-1);
}

int main()
{
    long long N;
    cin  >> N;
    cout << Fact1(N);
    return 0;
}
